<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="crosslog_return_state_view_tree" model="ir.ui.view">
        <field name="name">crosslog.return.state.view.tree</field>
        <field name="model">crosslog.return.state</field>
        <field name="arch" type="xml">
            <list string="Crosslog returns state" editable="bottom">
                <field name="name"/>
                <field name="code"/>
            </list>
        </field>
    </record>

    <record id="crosslog_return_state_action" model="ir.actions.act_window">
        <field name="name">Crosslog returns state</field>
        <field name="res_model">crosslog.return.state</field>
        <field name="view_mode">list</field>
    </record>

</odoo>