<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <data noupdate="1">

        <!-- Cleaning -->
        <record id="cron_remove_salesupply_finished_synchronizations" model="ir.cron">
            <field name="name">SALESUPPLY : Removing finished synchronizations</field>
            <field name="model_id" ref="model_salesupply_synchronization"/>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="state">code</field>
            <field name="code">model.remove_finished_syncrhonizations()</field>
        </record>
        <record id="cron_remove_salesupply_finished_synchronizations" model="ir.cron">
            <field name="name">SALESUPPLY : Removing older logs</field>
            <field name="model_id" ref="model_salesupply_synchronization"/>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="state">code</field>
            <field name="code">model.remove_older_logs()</field>
        </record>

        <!-- Synchronizations -->
    
    </data>
    

</odoo>
