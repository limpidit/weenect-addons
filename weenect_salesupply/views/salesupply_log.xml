<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="salesupply_log_view_tree" model="ir.ui.view">
        <field name="name">salesupply.log.view.tree</field>
        <field name="model">salesupply.log</field>
        <field name="arch" type="xml">
            <tree string="Data retrieving from Salesupply history">
                <field name="name" />
                <field name="execution_date" />
                <field name="type" widget="badge" 
                    decoration-info="type == 'info'" 
                    decoration-danger="type == 'error'"
                    decoration-warning="type == 'warning'"/>
            </tree>
        </field>
    </record>

    <record id="salesupply_log_view_form" model="ir.ui.view">
        <field name="name">salesupply.log.view.form</field>
        <field name="model">salesupply.log</field>
        <field name="arch" type="xml">
            <form string="">
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1><field name="name" placeholder="e.g. API connection"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="execution_date"/>
                            <field name="type" widget="badge" 
                                decoration-info="type == 'info'" 
                                decoration-danger="type == 'error'"
                                decoration-warning="type == 'warning'"/>
                            <field name="message"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="salesupply_log_action" model="ir.actions.act_window">
        <field name="name">Data retrieving from Salesupply history</field>
        <field name="res_model">salesupply.log</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p>
                See there the last executions of data retrieving from Salesupply to Odoo.
            </p>
        </field>
    </record>

</odoo>
